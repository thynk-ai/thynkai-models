# Benchmarks

This folder contains benchmark registry entries used by model performance notes.

Minimum:
- `benchmark-registry.json` — an index of benchmark ids and basic metadata.

Optional:
- `benchmarks/<benchmark-id>/` — additional benchmark docs or specs.
